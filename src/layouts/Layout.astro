---
// Layout.astro - Layout principal con Header integrado - Compatible con diseño naranja
import Header from "../components/Header.astro";

export interface Props {
	title: string;
	description?: string;
	keywords?: string;
	ogImage?: string;
	canonical?: string;
}

const { 
	title, 
	description = "Sur Occidente - Tesoros únicos del pasado",
	keywords = "ropa vintage, camisetas retro, vinilos clásicos, accesorios vintage, moda años 80, años 90, años 2000",
	ogImage = "/images/og-default.jpg",
	canonical
} = Astro.props;

const currentUrl = canonical || Astro.url.href;
const siteUrl = "https://sur-occidente.com";
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="keywords" content={keywords} />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="robots" content="index, follow" />
		<meta name="author" content="Sur Occidente" />
		
		<!-- Open Graph / Facebook -->
		<meta property="og:type" content="website" />
		<meta property="og:url" content={currentUrl} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={`${siteUrl}${ogImage}`} />
		<meta property="og:site_name" content="Sur Occidente" />
		<meta property="og:locale" content="es_CO" />
		
		<!-- Twitter -->
		<meta property="twitter:card" content="summary_large_image" />
		<meta property="twitter:url" content={currentUrl} />
		<meta property="twitter:title" content={title} />
		<meta property="twitter:description" content={description} />
		<meta property="twitter:image" content={`${siteUrl}${ogImage}`} />
		
		<!-- Canonical URL -->
		<link rel="canonical" href={currentUrl} />
		
		<!-- Favicon -->
		<link rel="icon" type="image/png" href="/favicon.png" />
		<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
		
		<!-- Preconnect for performance -->
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link rel="preconnect" href="https://trae-api-us.mchost.guru" />
		
		<!-- Google Fonts -->
		<link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500;1,600;1,700;1,800&display=swap" rel="stylesheet" />
		
		<!-- JSON-LD Structured Data -->
		<script type="application/ld+json">
			{
				"@context": "https://schema.org",
				"@type": "Organization",
				"name": "Sur Occidente",
				"url": "https://sur-occidente.com",
				"logo": "https://sur-occidente.com/images/logo.png",
				"description": "Tienda de ropa vintage, vinilos clásicos y accesorios retro. Tesoros únicos del pasado.",
				"address": {
					"@type": "PostalAddress",
					"addressCountry": "CO",
					"addressLocality": "Colombia"
				},
				"contactPoint": {
					"@type": "ContactPoint",
					"telephone": "+57-300-123-4567",
					"contactType": "customer service",
					"availableLanguage": "Spanish"
				},
				"sameAs": [
					"https://www.instagram.com/sur_occidente",
					"https://www.facebook.com/sur.occidente",
					"https://www.tiktok.com/@sur_occidente"
				]
			}
		</script>
		
		<title>{title}</title>
	</head>
	<body>
		<!-- Barras laterales negras -->
		<div class="sidebar-left">
			<span class="sidebar-text">SUR OCCIDENTE</span>
			<span class="sidebar-text">SUR OCCIDENTE</span>
			<span class="sidebar-text">SUR OCCIDENTE</span>
			<span class="sidebar-text">SUR OCCIDENTE</span>
		</div>
		<div class="sidebar-right">
			<span class="sidebar-text">SUR OCCIDENTE</span>
			<span class="sidebar-text">SUR OCCIDENTE</span>
			<span class="sidebar-text">SUR OCCIDENTE</span>
			<span class="sidebar-text">SUR OCCIDENTE</span>
		</div>
		
		<Header />
		<slot />
		
		<!-- Botón Scroll to Top -->
		<button id="scrollToTop" class="scroll-to-top" aria-label="Volver al inicio">
			<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
				<path d="M7 14L12 9L17 14" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
			</svg>
		</button>
		
		<script>
			// Funcionalidad del botón Scroll to Top
			document.addEventListener('DOMContentLoaded', function() {
				const scrollToTopBtn = document.getElementById('scrollToTop');
				
				// Mostrar/ocultar botón según scroll
				window.addEventListener('scroll', function() {
					if (window.pageYOffset > 300) {
						scrollToTopBtn.classList.add('visible');
					} else {
						scrollToTopBtn.classList.remove('visible');
					}
				});
				
				// Scroll suave al hacer click
				scrollToTopBtn.addEventListener('click', function() {
					window.scrollTo({
						top: 0,
						behavior: 'smooth'
					});
				});
			});
		</script>
	</body>
</html>

<style is:global>
	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
	}

	html {
		font-family: "Montserrat", "Arial", "Helvetica", sans-serif;
		/* Base neutra - el color lo maneja cada componente */
		background: #1a1a1a;
	}

	body {
		margin: 0;
		min-height: 100vh;
		/* Sin fondo fijo aquí - cada página define su estilo */
		color: #333;
		line-height: 1.6;
	}

	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}

	/* ESTILOS GLOBALES MODERNOS */
	h1,
	h2,
	h3,
	h4,
	h5,
	h6 {
		font-family: "Montserrat", Arial, sans-serif;
		font-weight: 800;
		font-style: italic;
		color: inherit; /* Hereda el color del contenedor padre */
	}

	p {
		line-height: 1.6;
		color: inherit;
	}

	a {
		color: #dc2626;
		transition: color 0.3s ease;
		text-decoration: none;
	}

	a:hover {
		color: #ef4444;
	}

	/* UTILIDADES GLOBALES */
	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 1rem;
	}

	.text-center {
		text-align: center;
	}

	.mb-1 { margin-bottom: 1rem; }

	/* Barras laterales negras */
	.sidebar-left,
	.sidebar-right {
		position: fixed;
		top: 0;
		height: 100vh;
		width: 60px;
		background: #000;
		z-index: 1000;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: space-evenly;
		writing-mode: horizontal-tb;
		text-orientation: mixed;
		overflow: hidden;
	}

	.sidebar-left {
		left: 0;
	}

	.sidebar-right {
		right: 0;
	}

	.sidebar-text {
		color: #F5DEB3;
		font-weight: bold;
		font-size: 0.7rem;
		letter-spacing: 1px;
		transform: rotate(90deg);
		margin: 30px 0;
		white-space: nowrap;
	}

	/* Ajustar el contenido principal para las barras laterales */
	body {
		padding-left: 60px;
		padding-right: 60px;
	}

	/* Responsive - ocultar barras en móviles */
	@media (max-width: 768px) {
		.sidebar-left,
		.sidebar-right {
			display: none;
		}
		
		body {
			padding-left: 0;
			padding-right: 0;
		}
	}
	.mb-2 { margin-bottom: 2rem; }
	.mb-3 { margin-bottom: 3rem; }

	.mt-1 { margin-top: 1rem; }
	.mt-2 { margin-top: 2rem; }
	.mt-3 { margin-top: 3rem; }

	/* SCROLL SUAVE */
	html {
		scroll-behavior: smooth;
	}

	/* MEJORAS DE ACCESIBILIDAD */
	*:focus {
		outline: 3px solid #dc2626;
		outline-offset: 2px;
	}

	/* OPTIMIZACIONES DE RENDIMIENTO */
	img {
		max-width: 100%;
		height: auto;
		display: block;
	}

	/* ANIMACIÓN REDUCIDA PARA USUARIOS QUE LO PREFIEREN */
	@media (prefers-reduced-motion: reduce) {
		*,
		*::before,
		*::after {
			animation-duration: 0.01ms !important;
			animation-iteration-count: 1 !important;
			transition-duration: 0.01ms !important;
		}
		
		html {
			scroll-behavior: auto;
		}
	}

	/* DARK MODE OPCIONAL */
	@media (prefers-color-scheme: dark) {
		/* Mantener el diseño naranja incluso en dark mode */
		html {
			background: #0a0a0a;
		}
	}

	/* HIGH CONTRAST MODE */
	@media (prefers-contrast: high) {
		a {
			color: #F5DEB3;
			text-decoration: underline;
		}
		
		a:hover {
			color: #dc2626;
		}
	}

	/* SELECCIÓN DE TEXTO PERSONALIZADA */
	::selection {
		background: rgba(220, 38, 38, 0.3);
		color: #F5DEB3;
	}

	::-moz-selection {
		background: rgba(220, 38, 38, 0.3);
		color: #F5DEB3;
	}

	/* BOTÓN SCROLL TO TOP */
	.scroll-to-top {
		position: fixed;
		bottom: 2rem;
		right: 2rem;
		width: 50px;
		height: 50px;
		background: #dc2626;
		color: #F5DEB3;
		border: none;
		border-radius: 50%;
		cursor: pointer;
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: 0 4px 12px rgba(220, 38, 38, 0.3);
		transition: all 0.3s ease;
		z-index: 1000;
		opacity: 0;
		visibility: hidden;
		transform: translateY(20px);
		/* Borde neón sutil para llamar la atención */
		border: 2px solid rgba(220, 38, 38, 0.6);
		box-shadow: 
			0 4px 12px rgba(220, 38, 38, 0.3),
			0 0 10px rgba(220, 38, 38, 0.4),
			inset 0 0 10px rgba(220, 38, 38, 0.1);
	}

	.scroll-to-top.visible {
		opacity: 1;
		visibility: visible;
		transform: translateY(0);
	}

	.scroll-to-top:hover {
		background: #ef4444;
		transform: translateY(-2px);
		box-shadow: 
			0 6px 16px rgba(220, 38, 38, 0.4),
			0 0 15px rgba(220, 38, 38, 0.6),
			inset 0 0 15px rgba(220, 38, 38, 0.2);
		border-color: rgba(220, 38, 38, 0.8);
	}

	.scroll-to-top:active {
		transform: translateY(-1px) scale(0.95);
	}

	/* Responsive */
	@media (max-width: 768px) {
		.scroll-to-top {
			bottom: 1.5rem;
			right: 1.5rem;
			width: 45px;
			height: 45px;
		}
	}
</style>

<!-- 
NOTAS DEL LAYOUT ACTUALIZADO:

✅ CAMBIOS PRINCIPALES:
- Fondo base neutro (no interfiere con el diseño naranja)
- Tipografía moderna (Arial en lugar de Georgia)
- Colores heredables para flexibilidad
- Sin gradientes fijos que compitan con el diseño

✅ COMPATIBILIDAD:
- Mantiene el Header original sin cambios
- Compatible con el nuevo diseño naranja vibrante
- Responsive y accesible
- Optimizado para performance

✅ CARACTERÍSTICAS:
- Scroll suave
- Utilidades CSS básicas
- Soporte para prefers-reduced-motion
- Selección de texto personalizada
- Dark mode y high contrast preparados

Para usar este layout con tu nueva página:
1. Reemplaza el Layout.astro actual con esta versión
2. El header se mantiene igual
3. El Body.astro nuevo maneja su propio fondo naranja
4. Todo funciona de forma integrada
-->